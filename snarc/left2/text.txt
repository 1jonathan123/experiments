<- <- <- Left <-- <- <- <- <- <--
The parity of the number 1 is

left <- <- left <- left Left <- Left Left
The parity of the number 2 is

Left <- <- <- Left Left Left Left <- <-
The parity of the number 3 is

Left <-- left <- <-- <-- Left <- left <--
The parity of the number 4 is

left <- left <-- Left <- <- <-- <-- <-
The parity of the number 5 is

<- <-- Left <-- Left <-- <-- left Left left
The parity of the number 6 is

<- Left left <- left Left Left left <- <-
The parity of the number 7 is

left <- <-- Left Left <-- left Left left <--
The parity of the number 8 is

<- <-- left Left <-- Left left Left Left Left
The parity of the number 9 is

<- <-- Left Left <-- <-- <- <-- left left
The parity of the number 1 is

left <-- <-- Left <-- left Left left <-- <-
The parity of the number 2 is

<-- Left <- left left <-- <- left left <-
The parity of the number 3 is

left <- <-- Left <-- left <-- <-- left left
The parity of the number 4 is

<-- Left <- <-- left left left left <- left
The parity of the number 5 is

left Left <- <- Left <-- <-- <- <-- left
The parity of the number 6 is

<- <-- left <-- left left left Left Left <-
The parity of the number 7 is

<- left left <- left <- <- Left <- Left
The parity of the number 8 is

<- <-- left <-- <- left Left left left left
The parity of the number 9 is

left <-- <-- Left left left Left left <- Left
The parity of the number 1 is

<-- Left left <- left <-- Left <- Left <-
The parity of the number 2 is

<- Left <- Left <-- <- left <-- <-- <--
The parity of the number 3 is

Left <- left Left <- left <-- <- Left <--
The parity of the number 4 is

Left left left left Left <-- <-- left <-- <-
The parity of the number 5 is

Left <-- left <- left left Left left <-- left
The parity of the number 6 is

Left <-- <-- Left <- left <-- <-- left <--
The parity of the number 7 is

<-- <- <-- Left Left left left Left left left
The parity of the number 8 is

Left <- <-- <-- left Left <-- <- <- <--
The parity of the number 9 is

Left <-- <- <- <- left left left left <-
The parity of the number 1 is

Left left Left left left <-- Left Left left <--
The parity of the number 2 is

<- <- <- Left <- <- Left Left left <--
The parity of the number 3 is

left <-- <-- left left left left Left <-- <--
The parity of the number 4 is

<-- left <-- <-- left <-- Left left Left left
The parity of the number 5 is

<- left <- <-- left Left <-- Left <-- left
The parity of the number 6 is

Left left <- <- left <- left <- <-- <--
The parity of the number 7 is

Left Left <- <- <-- left <- left <- <--
The parity of the number 8 is

Left <- Left Left <-- <-- left Left left <-
The parity of the number 9 is

<- Left left Left left <-- <-- <-- Left <--
The parity of the number 1 is

left left <- <-- <-- <- <-- <-- <- Left
The parity of the number 2 is

<-- left <-- left Left left left left <-- <-
The parity of the number 3 is

<- <- left Left <-- left Left <-- <- <--
The parity of the number 4 is

<- <- <- <-- <- Left Left left left left
The parity of the number 5 is

<-- Left <-- <- Left <- left left <- left
The parity of the number 6 is

left left left <-- <-- <-- <-- <-- left left
The parity of the number 7 is

<-- Left <- Left <-- <-- left Left left Left
The parity of the number 8 is

left Left <-- <-- left Left Left left Left Left
The parity of the number 9 is

<-- left left <-- left Left left <- <-- left
The parity of the number 1 is

Left left Left <- <-- <- left <- <- left
The parity of the number 2 is

left <-- <- <-- left Left <-- <- left Left
The parity of the number 3 is

Left <-- left left Left <- <- left <-- <-
The parity of the number 4 is

<- left <- <-- Left left left <-- Left <--
The parity of the number 5 is

<-- <-- left left <-- left left <-- left Left
The parity of the number 6 is

left left Left Left left <- <-- <-- <- Left
The parity of the number 7 is

<-- left Left left <-- left left left left Left
The parity of the number 8 is

<- Left <-- left left Left left <-- left left
The parity of the number 9 is

left <-- <-- <-- <-- <- Left left <- left
The parity of the number 1 is

<- <-- left <-- <-- <- <-- left <-- left
The parity of the number 2 is

left Left Left left left <-- Left <- Left left
The parity of the number 3 is

left <-- <-- <-- Left Left Left <-- Left <-
The parity of the number 4 is

left <-- <- <- <- left Left Left left Left
The parity of the number 5 is

<- left Left Left <-- <- <-- Left Left left
The parity of the number 6 is

left left left Left <-- <- Left left <-- <--
The parity of the number 7 is

Left Left Left <- <-- <-- <- <-- Left <-
The parity of the number 8 is

<- <-- <- <-- <-- <-- <-- <- <-- left
The parity of the number 9 is

<-- Left left <-- left Left <-- left <- left
The parity of the number 1 is

left <-- <- <-- left <-- <- Left <-- Left
The parity of the number 2 is

Left <-- <-- <- <-- <-- <-- left <- <--
The parity of the number 3 is

<- <-- <-- <-- <- <- left <-- left left
The parity of the number 4 is

left Left left Left Left <- <-- Left Left <-
The parity of the number 5 is

Left <- <- Left Left <- Left Left <-- <--
The parity of the number 6 is

<- Left <- <- <-- <-- Left <- Left <-
The parity of the number 7 is

<-- <-- left Left <- <- Left <- left <-
The parity of the number 8 is

<- <-- Left <- Left <- left <-- Left left
The parity of the number 9 is

<- <-- Left Left <-- left Left left left <--
The parity of the number 1 is

Left left Left <-- <-- left <-- <-- <-- left
The parity of the number 2 is

<- <-- <- left <- <- <-- Left <-- Left
The parity of the number 3 is

<-- left Left Left <-- left left Left left <--
The parity of the number 4 is

Left Left <- Left left <- left <- Left left
The parity of the number 5 is

Left Left Left Left <-- <-- Left Left <-- <--
The parity of the number 6 is

<- <-- <-- Left <- Left Left <- <-- <--
The parity of the number 7 is

Left Left <- Left left <-- <- left Left Left
The parity of the number 8 is

Left <- <- Left <-- <-- left <-- left left
The parity of the number 9 is

<-- Left left Left <-- <- Left left Left left
The parity of the number 1 is

left <- <-- Left Left <- Left <-- <- <--
The parity of the number 2 is

Left <-- <- <-- <-- Left left <-- <- left
The parity of the number 3 is

left left left <-- Left left Left <-- <-- Left
The parity of the number 4 is

<- <- <-- Left left left <-- left left left
The parity of the number 5 is

Left <- <- <- <- Left <- <- Left left
The parity of the number 6 is

<- <-- <- <- Left Left Left left Left left
The parity of the number 7 is

<- Left <- <- <- left <- Left <-- <-
The parity of the number 8 is

<-- <- left <-- left <-- <-- left left <-
The parity of the number 9 is

Left left left <- Left <-- left <- left left
The parity of the number 1 is

left left Left <- <-- <- left <- left <--
The parity of the number 2 is

<-- <-- left <- left <-- <- <- <-- left
The parity of the number 3 is

<-- <-- <-- Left <-- left <-- left <- <-
The parity of the number 4 is

left left <-- left <- <- <- left left <--
The parity of the number 5 is

<- <-- left <- <- left Left <-- Left <--
The parity of the number 6 is

<- <-- Left left Left <- left <-- left Left
The parity of the number 7 is

<- left left <-- Left <- left <-- <- Left
The parity of the number 8 is

left <-- <-- left left <-- <-- Left <- Left
The parity of the number 9 is

Left left Left left left <- Left <-- <-- left
The parity of the number 1 is

<-- Left Left left Left <-- Left left left Left
The parity of the number 2 is

<- left Left <-- <- Left Left <- <- <--
The parity of the number 3 is

Left left left <-- <- <- <- left <- Left
The parity of the number 4 is

<-- <-- Left <-- <-- Left Left left left <-
The parity of the number 5 is

<-- left <- Left <- <- <-- <-- Left <-
The parity of the number 6 is

<-- <-- Left <-- <-- <- <- Left <- <--
The parity of the number 7 is

<-- left <- left <-- left <- Left <-- <-
The parity of the number 8 is

<- left left left <- <-- <-- left Left <-
The parity of the number 9 is

<- <- <- Left Left <-- <- <- left Left
The parity of the number 1 is

<-- <- <-- Left left left <-- <- <-- <--
The parity of the number 2 is

<-- left <- Left left <-- Left Left left <-
The parity of the number 3 is

<-- left <-- Left <-- <- <- left left left
The parity of the number 4 is

left Left Left <-- <-- left Left Left <- Left
The parity of the number 5 is

Left left <-- Left left <-- <-- left <- <-
The parity of the number 6 is

left <-- <- <-- <- left Left <-- <-- <--
The parity of the number 7 is

Left <-- <-- left <-- <- <-- <-- <- left
The parity of the number 8 is

left <-- <-- <- Left <-- left <- Left left
The parity of the number 9 is

left Left left <- <-- <-- left left <- <--
The parity of the number 1 is

left left <- Left <- <-- <- left <-- Left
The parity of the number 2 is

<-- <-- left <- <- Left left <- left Left
The parity of the number 3 is

<-- <- left <-- <- <-- <- <- <-- <-
The parity of the number 4 is

Left left left Left <-- Left Left Left Left Left
The parity of the number 5 is

left <- Left left <-- left <-- left Left <-
The parity of the number 6 is

<- left <- <-- left Left left Left <- left
The parity of the number 7 is

Left <-- left left left <-- <-- <-- left left
The parity of the number 8 is

<- <- <-- Left left left Left <-- <- <--
The parity of the number 9 is

<-- <-- left Left Left left <- <-- Left Left
The parity of the number 1 is

Left <- left left <-- Left left Left <- Left
The parity of the number 2 is

Left Left Left Left <-- <-- <- <-- <- left
The parity of the number 3 is

left <- <- <-- <- <-- Left <- <-- <-
The parity of the number 4 is

<-- <- left <-- <- left <-- left <- Left
The parity of the number 5 is

<- <-- left Left left <- <-- Left left Left
The parity of the number 6 is

<- <-- Left Left <- <- <- left <- Left
The parity of the number 7 is

<-- left <-- <- Left Left <-- <-- <-- <--
The parity of the number 8 is

Left left Left <- <-- <- Left <- Left <--
The parity of the number 9 is

<-- <- <- left left <- <-- left Left left
The parity of the number 1 is

left <-- <- Left <-- <- <-- <-- left Left
The parity of the number 2 is

Left <-- left <-- left Left <-- left Left <--
The parity of the number 3 is

<- left left <-- <-- <-- left Left left left
The parity of the number 4 is

<- Left Left <-- <- left <- Left <- <-
The parity of the number 5 is

<- <-- left left <-- left left Left Left <-
The parity of the number 6 is

<- left <- <-- left left Left left <- Left
The parity of the number 7 is

Left <- left Left left left left <- <- <-
The parity of the number 8 is

left left left <-- <- <- left <-- <- <--
The parity of the number 9 is

<- <- <- left Left Left <-- left <-- left
The parity of the number 1 is

<-- <- left Left left Left <- left <-- left
The parity of the number 2 is

Left <- left <- <-- <-- left <-- Left left
The parity of the number 3 is

<- Left <- <-- <- <-- <-- Left Left <--
The parity of the number 4 is

<-- left Left <-- <-- <- left <-- <- <--
The parity of the number 5 is

left <- Left <-- <-- left left left <-- Left
The parity of the number 6 is

left <- <- left <-- left <- <-- Left <--
The parity of the number 7 is

left left <-- Left Left <- <-- left Left Left
The parity of the number 8 is

left Left Left <-- left <-- <- Left Left Left
The parity of the number 9 is

left <- left <-- left <- left <-- left <-
The parity of the number 1 is

<- <- left Left left <-- left Left <-- left
The parity of the number 2 is

<-- left left <- <- <-- Left <- Left left
The parity of the number 3 is

Left <-- <- <-- Left <-- <- <-- <-- <-
The parity of the number 4 is

Left <- Left <- Left <- <-- Left <-- Left
The parity of the number 5 is

<- <- <- Left left Left left <-- left <--
The parity of the number 6 is

<-- Left Left <-- left left Left <- left <--
The parity of the number 7 is

<- <- left left <-- <- <-- <- <- left
The parity of the number 8 is

left Left Left <-- Left Left <- Left Left <--
The parity of the number 9 is

<-- Left <-- left <-- left Left left <-- left
The parity of the number 1 is

Left <-- left Left Left Left Left <- left <-
The parity of the number 2 is

<-- Left <- <-- left <- <- <- <-- <-
The parity of the number 3 is

Left <-- Left Left Left <- left left left <-
The parity of the number 4 is

Left left Left left <-- <- <- <- Left <--
The parity of the number 5 is

<- left left Left <-- <- left <- <- <--
The parity of the number 6 is

<-- <-- left <-- <-- Left left Left <- <-
The parity of the number 7 is

<- Left Left left left <- <-- Left left <--
The parity of the number 8 is

Left <- Left left left <-- <- left <- left
The parity of the number 9 is

<- <- <- Left <-- Left Left <- <-- left
The parity of the number 1 is

Left <- Left <- left left <- <-- <-- <-
The parity of the number 2 is

Left Left left <-- <- Left <- left <-- <-
The parity of the number 3 is

Left <-- left <-- Left <- <- <-- left <--
The parity of the number 4 is

<-- Left left <-- <-- <-- <- left <-- left
The parity of the number 5 is

<-- left <-- left <- Left <-- <- Left <--
The parity of the number 6 is

left left Left <- <-- <-- <- Left <-- <-
The parity of the number 7 is

<-- left <-- <- <-- left <-- left Left Left
The parity of the number 8 is

left <-- <-- Left <- <-- Left <-- Left Left
The parity of the number 9 is

left Left <-- <-- Left <-- left left <- <-
The parity of the number 1 is

<-- <-- <- <- <- Left Left <- Left Left
The parity of the number 2 is

left <- left left Left Left <-- Left Left left
The parity of the number 3 is

left <- <-- left Left <- left left <- Left
The parity of the number 4 is

<- <- left <-- <- left <- <- <- Left
The parity of the number 5 is

Left left left left <- left <- left left <-
The parity of the number 6 is

Left <-- <-- <- <- left <-- left <- left
The parity of the number 7 is

<-- left Left <- <-- left <-- left <-- <--
The parity of the number 8 is

<-- <- <- <- left <- <- left <- <--
The parity of the number 9 is

<-- Left left left Left <-- Left <-- <- <--
The parity of the number 1 is

Left <-- left left Left <-- Left left <-- Left
The parity of the number 2 is

<- <- <-- left left <- <- <-- <- Left
The parity of the number 3 is

<- <- <- left left <- Left left left Left
The parity of the number 4 is

<-- <- <-- <-- <- <- left left <-- Left
The parity of the number 5 is

<- <- <-- Left Left <- left <-- <- left
The parity of the number 6 is

Left <- <-- <-- Left <-- left Left <-- <--
The parity of the number 7 is

<-- <- <- <- left <-- <- left <-- Left
The parity of the number 8 is

Left Left <-- Left left <-- <- <- <- Left
The parity of the number 9 is

<- <-- left left Left Left Left Left <-- <-
The parity of the number 1 is

<-- <- <-- <- Left Left <-- <-- <-- <--
The parity of the number 2 is

Left <-- Left Left Left <-- <-- left <-- <--
The parity of the number 3 is

Left left <-- left <-- <- <- Left Left left
The parity of the number 4 is

<- Left <- <-- Left <-- Left Left Left <--
The parity of the number 5 is

<- <- <- Left <- <-- left Left <-- <--
The parity of the number 6 is

Left <- <-- <-- <- Left left <- <- left
The parity of the number 7 is

Left <- Left left left <- left left <- Left
The parity of the number 8 is

<- <- Left Left <-- <- <- <- <-- <-
The parity of the number 9 is

Left <- <-- left Left left <- <- left left
The parity of the number 1 is

<-- <- <-- <-- <-- <- Left <- <-- <-
The parity of the number 2 is

<- left <-- <-- left left <- <-- left left
The parity of the number 3 is

<- <- <-- Left Left <-- <-- left left <-
The parity of the number 4 is

<- Left <- Left left Left <- <- left <--
The parity of the number 5 is

left <- left left Left <-- <- Left <-- left
The parity of the number 6 is

left <-- Left Left left <-- <- left <-- left
The parity of the number 7 is

<- Left Left <- <- left <- <-- <- left
The parity of the number 8 is

left <-- <- <- <-- Left <- <- <-- left
The parity of the number 9 is

Left left left <-- left <- left left <-- left
The parity of the number 1 is

<-- <-- <-- <-- Left Left left <-- left <-
The parity of the number 2 is

left Left Left <-- <-- left left left <-- <--
The parity of the number 3 is

<- left Left Left Left <-- <- left left <-
The parity of the number 4 is

<-- <-- <- left left <- <- <-- Left <--
The parity of the number 5 is

left <- left <-- <-- Left <- <-- <-- <-
The parity of the number 6 is

<- <- <-- <-- <-- left left <- left <-
The parity of the number 7 is

Left Left left <- <-- <-- left <- <- <--
The parity of the number 8 is

left <-- Left left Left left Left <-- <- <-
The parity of the number 9 is

Left left <- <- <-- <- <- <- <-- left
The parity of the number 1 is

<- <- Left <-- <- Left Left <-- <-- Left
The parity of the number 2 is

Left <-- Left <- left Left left <- <- left
The parity of the number 3 is

<-- <-- left left <-- <- Left <- <- Left
The parity of the number 4 is

Left <-- <-- left <-- left <-- <- Left left
The parity of the number 5 is

<- Left <- Left <-- <- <- <-- <- <--
The parity of the number 6 is

left <- left left <-- <-- Left Left left Left
The parity of the number 7 is

<-- <-- Left <- <-- <-- <-- <-- <-- Left
The parity of the number 8 is

<- <- Left <-- <-- Left left <- left <--
The parity of the number 9 is

Left Left Left Left left left <-- <- left Left
The parity of the number 1 is

Left left left left <-- <- <- Left <- left
The parity of the number 2 is

<- <-- <- Left <-- <-- Left <-- <-- left
The parity of the number 3 is

left <-- left <-- Left <-- <- <-- Left <-
The parity of the number 4 is

left left Left <- <-- <-- <-- left <- left
The parity of the number 5 is

left left left Left left <-- <-- Left left left
The parity of the number 6 is

<- left left <- <-- left <- <- <- Left
The parity of the number 7 is

left <-- <-- <- left Left <-- <-- Left <--
The parity of the number 8 is

<- Left left left Left <-- <-- <- left <--
The parity of the number 9 is

Left Left Left Left Left left <- <-- <- left
The parity of the number 1 is

left <-- Left Left <- <- Left Left <- left
The parity of the number 2 is

Left <- Left <- Left <- Left left Left <-
The parity of the number 3 is

left <- left Left <- <-- <-- left <-- Left
The parity of the number 4 is

left <- <-- <-- <-- left <- <- <- Left
The parity of the number 5 is

<-- <- <-- Left Left Left <- Left <- <--
The parity of the number 6 is

left left Left left <- Left <- <-- <- left
The parity of the number 7 is

left <-- <-- left Left <- Left <-- <-- <-
The parity of the number 8 is

<-- <- left Left <- left <- left <- left
The parity of the number 9 is

<-- <- left Left <- <- left Left Left <-
The parity of the number 1 is

Left left left Left <- left <- <- left Left
The parity of the number 2 is

left <- <-- <- <- <-- <- <- left <-
The parity of the number 3 is

Left left Left <-- left <-- left Left <-- <--
The parity of the number 4 is

<- left <-- <- <- <- left <-- Left Left
The parity of the number 5 is

<-- Left Left <- <- <-- left <- left Left
The parity of the number 6 is

<- <- <-- <- left Left Left left <- left
The parity of the number 7 is

Left <- <- left left <- Left Left <-- <-
The parity of the number 8 is

<- <-- left Left <-- Left left <-- <-- Left
The parity of the number 9 is

Left <-- <- Left Left Left <-- Left <-- left
The parity of the number 1 is

<- left left Left <-- Left Left <- <- <--
The parity of the number 2 is

<- Left left left left Left <- <- Left Left
The parity of the number 3 is

left Left <- Left <-- Left left Left <- <--
The parity of the number 4 is

left <-- <- left Left <-- Left left left left
The parity of the number 5 is

left <- <-- left <-- Left <-- left <-- left
The parity of the number 6 is

<-- left left <-- <- <- <-- <-- <- left
The parity of the number 7 is

Left <- <-- Left <-- <-- Left <- left left
The parity of the number 8 is

Left Left left Left <-- Left <- Left left <--
The parity of the number 9 is

<-- <-- Left Left <- <-- Left left left <--
The parity of the number 1 is

<-- <-- left <-- left left left <-- Left Left
The parity of the number 2 is

<- <- <- <-- <-- Left <-- <- <-- <-
The parity of the number 3 is

<-- left Left <-- left <- <-- <- <- left
The parity of the number 4 is

<-- <-- <-- Left <-- <-- <- left left <--
The parity of the number 5 is

<-- <-- Left Left Left left <- <- <- left
The parity of the number 6 is

<- <- left <-- Left <-- Left left left left
The parity of the number 7 is

left <- <-- left <- Left <- Left <-- <-
The parity of the number 8 is

Left <-- left Left <- <- <- Left left left
The parity of the number 9 is

left left <- left left Left <- left <- left
The parity of the number 1 is

Left left <-- left <-- left left left <- <-
The parity of the number 2 is

<-- Left <- Left left left Left Left left left
The parity of the number 3 is

Left Left Left <-- left <-- <- Left <- <-
The parity of the number 4 is

Left <-- left Left <-- Left left Left <-- <--
The parity of the number 5 is

<- <-- <-- <-- Left <- <-- left <-- <-
The parity of the number 6 is

<- <-- <-- left <-- Left left left <-- <-
The parity of the number 7 is

Left <- Left Left left left Left <- Left Left
The parity of the number 8 is

<-- Left <-- left Left Left <- <- <-- <--
The parity of the number 9 is

<-- <- <- Left <-- <-- <- <- left Left
The parity of the number 1 is

<-- left left <-- <-- Left <- Left <- Left
The parity of the number 2 is

Left <- Left Left <-- Left left Left <-- <--
The parity of the number 3 is

Left <- left Left Left <- Left <-- <- Left
The parity of the number 4 is

<-- <- <- <-- <- <- Left <-- left left
The parity of the number 5 is

left <- left left Left <- <- <- left left
The parity of the number 6 is

<- Left <- left <-- <-- <-- left left <-
The parity of the number 7 is

left Left <-- <-- Left Left <-- left <- Left
The parity of the number 8 is

Left Left <-- Left <-- <- Left Left Left left
The parity of the number 9 is

<- Left <-- <- <-- Left left left <- <--
The parity of the number 1 is

Left Left Left <-- left <- <- left <- left
The parity of the number 2 is

left <- <- <-- left <-- <- <- Left Left
The parity of the number 3 is

<-- <- <-- <-- <-- Left left <- Left <--
The parity of the number 4 is

<-- Left <- Left Left Left Left <-- <-- left
The parity of the number 5 is

left <-- Left left left Left <- Left Left <--
The parity of the number 6 is

<- <- <- Left Left Left left <-- Left <--
The parity of the number 7 is

left left left <-- left Left <-- left Left <-
The parity of the number 8 is

<- Left Left left <- Left <- <- <-- left
The parity of the number 9 is

Left <-- left <-- Left left left <- <-- <--
The parity of the number 1 is

left left <-- <- Left <-- <- <- <- left
The parity of the number 2 is

<- left Left <- left <- Left Left <-- Left
The parity of the number 3 is

left left Left left <-- left Left left Left left
The parity of the number 4 is

left left Left <-- <-- Left Left left Left <--
The parity of the number 5 is

left left Left <-- Left left <-- <- <-- Left
The parity of the number 6 is

<- Left left <- Left <- <- <-- left <--
The parity of the number 7 is

Left <- <-- left Left Left <-- <- <-- <-
The parity of the number 8 is

<- <- <- <- <-- left <- left <-- left
The parity of the number 9 is

Left left Left left <- left <-- left Left left
The parity of the number 1 is

Left <- <-- left left left <- <- <- Left
The parity of the number 2 is

Left Left Left Left left Left <-- <- <- left
The parity of the number 3 is

<- Left <-- <- <-- <- <-- <-- <- <-
The parity of the number 4 is

<-- <-- <-- left left <- Left left <-- Left
The parity of the number 5 is

Left left <-- <-- <- <-- <- Left <-- <--
The parity of the number 6 is

<- left Left <-- left Left <-- <-- <- left
The parity of the number 7 is

Left left <- <-- <- <- left Left <-- <--
The parity of the number 8 is

<- left <-- <-- left left Left <-- <- <-
The parity of the number 9 is

<- left <- <- Left <-- left Left Left <-
The parity of the number 1 is

left <- left <- Left left Left <- <-- left
The parity of the number 2 is

Left Left <-- left left <-- <- <- Left Left
The parity of the number 3 is

<-- <- Left left Left left <-- <-- left <-
The parity of the number 4 is

<- Left <-- Left left Left Left <- <-- <-
The parity of the number 5 is

<- <- left <- <- left <-- <- <- <-
The parity of the number 6 is

<- <- Left Left Left Left <-- left Left <-
The parity of the number 7 is

left <-- <- <-- left left <- <-- Left left
The parity of the number 8 is

left left <- <-- <- <- left <- Left <-
The parity of the number 9 is

<- <- Left Left <-- Left Left <- left <--
The parity of the number 1 is

<-- <- Left Left left <-- left Left Left <--
The parity of the number 2 is

Left <-- <-- left <- <-- <-- <- <-- <-
The parity of the number 3 is

Left <- <-- Left <- <- <-- left left Left
The parity of the number 4 is

left left left Left left <- Left <- left Left
The parity of the number 5 is

Left Left <- <-- Left left left <- <- Left
The parity of the number 6 is

left left left <- left <- <-- left <- left
The parity of the number 7 is

left <- Left <- <- <-- left left left Left
The parity of the number 8 is

<-- left <- Left <-- left <- <-- left Left
The parity of the number 9 is

<- Left <-- Left <-- <-- <-- left <-- Left
The parity of the number 1 is

<-- <-- <- left Left Left <- <- <- left
The parity of the number 2 is

left <-- <-- <-- <- left <- <-- Left left
The parity of the number 3 is

left <-- left <-- Left left left left Left <-
The parity of the number 4 is

left <-- left Left <-- Left Left <- Left Left
The parity of the number 5 is

<- Left left left <- left <- left <- <--
The parity of the number 6 is

Left left <-- left left <-- <-- <- <- <-
The parity of the number 7 is

Left left <- left left Left <- <-- <- <-
The parity of the number 8 is

Left Left <-- left Left left <-- <- left <--
The parity of the number 9 is

left <-- <-- Left <- left <-- left Left left
The parity of the number 1 is

left Left <-- <-- Left left <-- <-- <- <--
The parity of the number 2 is

Left left <- left left <-- <- left Left <-
The parity of the number 3 is

<- Left left <-- left left left Left Left Left
The parity of the number 4 is

Left left left <- <-- left Left left Left Left
The parity of the number 5 is

<-- <- <-- <- <-- <-- Left left <-- <-
The parity of the number 6 is

<-- Left left Left <- <-- <-- <- <- <-
The parity of the number 7 is

left Left Left <- Left <-- left Left <- left
The parity of the number 8 is

left <- Left <-- left <- <-- <- <- Left
The parity of the number 9 is

left left left left <-- Left left Left Left <-
The parity of the number 1 is

<- <-- <-- Left <- <- <- <-- <- left
The parity of the number 2 is

<-- left <- Left <- <- <-- left Left Left
The parity of the number 3 is

<- left <-- <-- left left Left Left left <-
The parity of the number 4 is

<- Left Left left <- left <-- <-- <-- left
The parity of the number 5 is

Left <-- <- <- <-- Left Left left <-- left
The parity of the number 6 is

left <- <-- <- Left left <-- <- Left <--
The parity of the number 7 is

left <-- <- <- left <- Left left <- <--
The parity of the number 8 is

Left left left <- Left <- <- Left Left <-
The parity of the number 9 is

<- <-- <-- <-- <- <- left <-- left Left
The parity of the number 1 is

left <-- <- <-- <- <- Left <- Left <-
The parity of the number 2 is

Left Left left <- Left left <- Left <-- Left
The parity of the number 3 is

<- <- left Left <- Left left Left left left
The parity of the number 4 is

Left <-- <- <-- <-- Left left <-- Left <--
The parity of the number 5 is

Left <-- Left <-- <-- <- <-- <-- left <-
The parity of the number 6 is

<- left Left <-- Left left left left left <-
The parity of the number 7 is

left <-- <- Left Left Left <- <-- <- left
The parity of the number 8 is

<- left <-- <- left left Left Left <-- <-
The parity of the number 9 is

left left Left <-- <- <- <-- <- left Left
The parity of the number 1 is

left <- <-- <-- <- Left <-- Left Left <--
The parity of the number 2 is

<- <- <-- <- <- Left <- <-- <-- left
The parity of the number 3 is

<- <- Left <-- <-- <-- left <- <-- <--
The parity of the number 4 is

Left left left <-- <- left <-- <- <-- Left
The parity of the number 5 is

left left Left Left Left <- <-- <-- Left left
The parity of the number 6 is

left left <- <-- <-- <-- Left Left <-- <-
The parity of the number 7 is

<-- left <-- <-- <-- left <- <-- Left left
The parity of the number 8 is

Left <-- Left <-- <- Left <- <-- <-- Left
The parity of the number 9 is

<-- left Left left <-- <-- <- <- left <-
The parity of the number 1 is

<- <- <-- <- left <- Left <-- <- Left
The parity of the number 2 is

left Left left left Left left <- left Left Left
The parity of the number 3 is

<-- <-- Left left Left Left left left left <-
The parity of the number 4 is

<- <- <- <-- left Left Left <- <- left
The parity of the number 5 is

Left Left <-- <- left Left Left <- Left Left
The parity of the number 6 is

left <-- Left left Left <-- left left Left left
The parity of the number 7 is

left <-- Left <- left Left <- <- <-- left
The parity of the number 8 is

<- <- left <- Left <-- left <-- left Left
The parity of the number 9 is

<- <- left <- left <-- left <- <-- left
The parity of the number 1 is

<- Left Left <- <-- left <- <-- <-- <-
The parity of the number 2 is

<-- left <-- <- <-- <- <- left <- Left
The parity of the number 3 is

left Left left <- left left <- <- <- <--
The parity of the number 4 is

<- left <- Left <- <- left <- <- <-
The parity of the number 5 is

<- <- Left <- <-- <- Left <- <-- <-
The parity of the number 6 is

left Left Left <- <-- <-- left left Left left
The parity of the number 7 is

Left Left <- <-- left <- left <-- <- <--
The parity of the number 8 is

left <- Left Left Left <- <-- <- <-- <--
The parity of the number 9 is

<- left left <- left <- <-- left <-- <--
The parity of the number 1 is

Left <-- <-- <-- left <- <- left Left <-
The parity of the number 2 is

left left Left Left <-- <-- <- <- <- <--
The parity of the number 3 is

<- <- <-- left <-- <-- left <- left Left
The parity of the number 4 is

<- <- <-- <- <- left Left left Left Left
The parity of the number 5 is

<-- left Left <- Left <- left <- <-- left
The parity of the number 6 is

<-- <-- Left Left left <-- <- Left <- <-
The parity of the number 7 is

<- Left <- <- Left <- <-- Left <- left
The parity of the number 8 is

left <- left left <-- left Left Left <- <-
The parity of the number 9 is

<- Left <-- <- Left <- left <-- <-- <--
The parity of the number 1 is

<- <-- left Left <-- left <-- left left <-
The parity of the number 2 is

<-- left Left <- <- Left <- <-- <-- left
The parity of the number 3 is

left Left <- Left left Left Left left Left Left
The parity of the number 4 is

<-- <-- Left <- <-- Left <-- <-- <-- <--
The parity of the number 5 is

<- <-- <-- left <-- Left left <- <-- <-
The parity of the number 6 is

<-- <-- <-- Left left left <-- <-- <-- <-
The parity of the number 7 is

<- <- <-- <-- left <- <- Left Left <--
The parity of the number 8 is

Left left <- <- <-- Left left Left left left
The parity of the number 9 is

Left Left <-- <- <-- <- <- Left Left <-
The parity of the number 1 is

<-- left left left <-- left <-- <-- left <--
The parity of the number 2 is

left left left Left <-- <- left Left <- left
The parity of the number 3 is

Left Left left <- left <-- <-- <- <- <-
The parity of the number 4 is

Left <-- left <- Left <-- left left left <--
The parity of the number 5 is

<-- <-- Left <-- left <- <-- <- Left <-
The parity of the number 6 is

Left <- <-- left <- left Left left <- Left
The parity of the number 7 is

left Left <- <- <- <- <- <- <-- left
The parity of the number 8 is

<-- left <-- <- Left <- <-- left left Left
The parity of the number 9 is

left <-- Left left Left left left <-- Left left
The parity of the number 1 is

Left Left Left left <- <-- left Left <-- Left
The parity of the number 2 is

<-- left <- <-- Left <-- left left <-- <-
The parity of the number 3 is

<- Left <- <-- <- <-- Left <- Left <--
The parity of the number 4 is

left left <- <-- left Left Left left <-- <--
The parity of the number 5 is

left <-- left Left <-- <- <- <-- <-- <--
The parity of the number 6 is

Left Left <- <- <-- left Left <- left Left
The parity of the number 7 is

Left <-- left left left left left Left <- <--
The parity of the number 8 is

left <-- Left left <-- <- <-- <-- <-- left
The parity of the number 9 is

left Left Left <-- Left <- <-- Left left <-
The parity of the number 1 is

<-- Left <- <-- left left Left Left left Left
The parity of the number 2 is

left left <-- left <- Left left <-- Left <--
The parity of the number 3 is

Left Left <-- Left Left Left left <- Left left
The parity of the number 4 is

<-- <- <-- <-- left <-- <- <- left left
The parity of the number 5 is

Left <-- left <- <-- Left <-- <-- <-- Left
The parity of the number 6 is

<- left <- left Left Left Left <- left Left
The parity of the number 7 is

<- <- <- Left <- <-- Left left <-- <--
The parity of the number 8 is

<-- left <- <-- <- <-- left left <- Left
The parity of the number 9 is

